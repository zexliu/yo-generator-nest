import { Injectable } from '@nestjs/common';
import { InjectModel } from '@nestjs/mongoose';
import { Model } from 'mongoose';
import { BaseService } from 'src/commen/base/base.service';

import { <%=className%>, <%=className%>Document } from './model/<%=modelName%>.model';

@Injectable()
export class <%=className%>Service extends BaseService<<%=className%>Document> {
  constructor(@InjectModel(<%=className%>.name) model: Model<<%=className%>Document>) {
    super(model);
  }
}
